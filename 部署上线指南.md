# 日程管家 · 上线指南（发链接给朋友用）

目标：把 `schedule-app` 部署到 Vercel，得到一个 `https://…vercel.app` 链接，发给朋友；朋友在**安卓手机**上用「添加到主屏幕」装到桌面，并支持**提前 30 分钟推送提醒**。

> 只有部署成在线链接才能装进手机桌面；直接发文件（zip）不能自动装到桌面。

---

## 0) 前提
- 一个免费 Vercel 账号：打开 [vercel.com](https://vercel.com) 用邮箱/GitHub 注册（免费）。

## 1) 部署（两种方式二选一）

### 方式一：Vercel CLI（本地文件夹，推荐）
在**项目目录**打开终端执行：

```bash
cd "D:\ds haness\schedule-app"

# ① 登录（第一次会弹出浏览器，点授权即可；本机没有登录令牌，需要你点一下）
npx vercel

# ② 正式上线
npx vercel --prod
```

- 第一次的登录只需做一次。
- 成功后会输出一个预览地址和正式地址（`https://…vercel.app`）。

### 方式二：Vercel 网页导入（需要先把项目推到 GitHub）
1. 把 `schedule-app` 文件夹推到一个 GitHub 仓库（`.vercelignore` 已排除 node_modules 等）。
2. vercel.com → **Add New → Project → Import** 该仓库，直接部署。

> 若 `npx vercel` 下载很慢/超时（本机实测会超时），请在**你自己的终端**运行（网络环境不同），或改用方式二/下面第 3 步。

## 2) 配置 Vercel KV（必须，推送提醒靠它）
Serverless 是无状态的，定时任务必须能从 KV 读到订阅与日程，否则推不了。

1. Vercel 项目 → **Storage → Create Database → Vercel KV** → 创建并 **Connect** 到本项目。
2. 连接后自动注入 `KV_REST_API_URL` / `KV_REST_API_TOKEN`，后端会自动读写，**无需改代码**。

> 也可以换：项目 **Settings → Environment Variables** 手动加 `KV_REST_API_URL` 和 `KV_REST_API_TOKEN`（在 KV 数据库详情里复制）。

## 3) （可选）VAPID 密钥
代码内置了一套可用密钥，可先用。想用自己的一套：
- 项目 **Settings → Environment Variables** 添加 `VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`（如 `mailto:you@example.com`）。
- 生成密钥：项目里运行 `npm run gen:vapid`。

改完环境变量后，重新部署一次（Vercel 项目 → Deployments → Redeploy）。

## 4) 把链接发给朋友（安卓手机）
发 `https://你的项目.vercel.app` 给朋友，并告诉他这两步：

1. 用 **Chrome** 打开链接（必须是 https，不能用文件/局域网 IP）。
2. 浏览器右上角菜单（⋮）→ **添加到主屏幕**（或浏览器弹出的「安装应用」提示）。
   - 桌面上就会出现「日程管家」图标，像原生 App 一样打开使用。
3. 想让「关闭 App 也能收到提醒」：打开 App → 点右上角铃铛 → 允许「通知」权限；
   首次**添加或修改日程**时会自动把日程同步到服务器，到点（提前 30 分钟）即推送提醒。

## 5) 常见问题
- **朋友在 iPhone 上？** 也能用：Safari 打开链接 → 分享 → **添加到主屏幕**。（iOS 不能装 APK，PWA 是唯一通用方式）
- **为什么不能用文件/zip？** 网页应用的「添加到主屏幕」和推送都需要 HTTPS + 在线服务器，本地文件做不到。
- **想打包成安卓 APK（一个可下载安装文件）？** 本机无安卓工具链，需要你补装 JDK + Android Studio，用 Capacitor 打包；且仅安卓、需允许「未知来源」。
