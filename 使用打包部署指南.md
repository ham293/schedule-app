# 日程管家 · 完整使用与分发指南

> 一个可在**手机（安卓）+ 电脑**上使用的日程整理工具：根据输入**自动生成表格**，并在开始前**提前提醒**（声音 + 震动 + 横幅，后台也行）。
> 项目位置：`D:\ds haness\schedule-app`

---

## 一、功能总览

| 能力 | 说明 |
| --- | --- |
| 单条自然语言 | `明天下午3点 开会`、`11:00 买书`、`下午2点半 上课` |
| 相对时间 | `11分钟后去买菜`、`2小时后开会`、`3天后交报告`、`现在/马上` |
| **批量解析通知** | 整段粘贴「9.3日程 / 时间：… / 地点：…」→ 自动拆成多条并生成表格 |
| 智能时间识别 | 上午/下午、X点半、24 小时制、**全角冒号 `：`**、中文数字（九点/两点半）、全天/整天 |
| 多天 / 同地点合并 | 通知含多个日期头自动分天；同地点首尾相连的日程自动合并 |
| 自动生成表格 | 列表视图（按日期分组、倒计时、状态）+ 表格视图（日期/时间/事项/提醒/状态） |
| 提前提醒 | 默认提前 30 分钟；App 打开时**弹窗+声音+震动**；**后台/锁屏用横幅+闹钟铃声+震动** |
| 手动/编辑/删除 | 地点、结束时间、备注、提醒时长均可改；一键标记完成 |
| 铃铛测试 | 点右上角铃铛立刻发一条「测试提醒」，验证通知是否正常 |

> 提醒音：内置自定义**闹钟铃声** `schedule_alarm`（响亮、连续），并设最高优先级 + 强震动。

---

## 二、在手机（安卓）上使用

1. 安装 App（打包成 APK，见第三节）。
2. 打开 → 粘贴通知到顶部输入框，点「**一键录入 / 批量解析**」→ 自动多条日程。
3. 或输入单条（如 `明天下午3点 开会`、`11分钟后去买菜`）。
4. 到点（提前提醒）→ **横幅 + 闹钟铃声 + 震动**，锁屏/退后台也会响。

### 华为/国产手机重点（必须做，否则后台提醒会被拦）
- 设置 → 应用管理 → **日程管家** → 通知：打开「允许通知」「锁屏通知」「横幅通知」，声音开。
- 应用启动管理 → 改**手动管理** → 勾选「允许自启动 / 允许后台活动」。
- **电池** → 设为**不受限制**；关闭免打扰/勿扰；调大通知音量。

---

## 三、打包成安卓 APK（发给朋友）

APK 版**不依赖任何域名/服务器**，朋友装上即可用，后台提醒也有效。

### 方式一：GitHub Actions 云端编译（推荐，不用装安卓软件）
1. 注册 [github.com](https://github.com)，新建仓库（如 `schedule-app`，Public）。
2. 上传项目（用命令行最稳）：
   ```
   cd "D:\ds haness\schedule-app"
   git init
   git add .
   git commit -m "init"
   git branch -M main
   git remote add origin https://github.com/你的用户名/schedule-app.git
   git push -u origin main
   ```
   > 首次 push 弹浏览器登录 GitHub；要 token 就在 GitHub 生成 Personal Access Token（勾 `repo`）后：
   > `git remote set-url origin https://用户名:token@github.com/用户名/schedule-app.git` 再 push。
3. push 后 GitHub 自动编译（仓库 **Actions** 标签 → Build APK，等变绿）。
4. 编译成功 → 该次运行**最下方 Artifacts → schedule-app-apk** → 下载 `.zip` → 解压出 `app-debug.apk`。
5. 把 `app-debug.apk` 微信/QQ 发给朋友 → 安装（允许未知来源）→ 即可用。

> 以后改代码：`git add . && git commit && git push` → Actions 自动重新编译 → 再下载新 APK。

### 方式二：本地用 Android Studio 编译
- 需要装 Android Studio（含 SDK/JDK）。官网被墙可用**国内镜像**。
- 打开 `D:\ds haness\schedule-app\apk\android` → Build → Build APK(s) → 得到 `app-debug.apk`。
- 若改了网页资源，先运行 `apk\build.ps1` 同步再 Build。

> APK 内已包含：自定义闹钟铃声（res/raw）、通知图标（res/drawable）、原生本地通知（Capacitor LocalNotifications）——确保后台也响铃+横幅。

---

## 四、在电脑上本地运行 / 预览

- **方式一（推荐）**：双击桌面「**日程管家**」快捷方式 → 自动启动服务并打开 `http://localhost:8099`。
- **方式二**：命令行运行
  ```
  cd "D:\ds haness\schedule-app"
  npm run dev          # 等价于 node server-local.js
  ```
  浏览器打开 `http://localhost:8099`。

> 通知在电脑浏览器上也能开启（localhost 属安全上下文）。

---

## 五、部署成网页版 PWA（可选，推荐朋友装 APK 就用不到这步）

如果想让朋友用**网页链接**打开并用（而非 APK），可部署到 Vercel：

1. 登录 [vercel.com](https://vercel.com)，在项目目录执行：`npx vercel --prod --yes`。
2. 得到 `https://xxx.vercel.app`，朋友用 Chrome 打开 → 添加到主屏幕。
3. **配置 Vercel KV**（必须，否则后台推送不存取）：Vercel 项目 → Storage → Vercel KV → Connect。
4. **提醒推送**：免费版 Vercel **不允许每分钟定时任务**（需 Pro），或改用**免费外部定时服务**（如 cron-job.org）每分钟访问 `https://xxx.vercel.app/api/remind` 来触发。

> ⚠️ 国内访问 `*.vercel.app` 常被限制，朋友在国内建议**用 APK**（方式三），不依赖域名。

---

## 六、目录结构

```
schedule-app/
├─ index.html                 # 主界面
├─ manifest.webmanifest       # PWA 清单
├─ sw.js                      # Service Worker（推送/通知/离线）
├─ vercel.json                # Vercel 配置（cron 已去，免费版）
├─ package.json               # 依赖(web-push) + 脚本
├─ css/style.css              # 样式
├─ js/
│  ├─ nlparse.js              # 自然语言/批量解析
│  └─ app.js                  # 应用逻辑 + 提醒 + 原生本地通知
├─ api/                       # Vercel 函数（vapid/subscribe/sync/remind）
├─ server-local.js            # 本地开发服务器
├─ start.js                   # 桌面快捷方式启动器
├─ icons/                     # 应用图标
├─ apk/                       # APK（Capacitor）工程
│  ├─ package.json            # Capacitor 依赖
│  ├─ capacitor.config.json   # appId/appName/webDir
│  ├─ build.ps1               # 一键：复制资源+加平台+同步
│  ├─ res-raw/schedule_alarm.wav    # 自定义闹钟铃声
│  ├─ res-drawable/ic_stat_icon.xml # 通知图标
│  └─ android/                # 安卓工程（cap add 生成）
├─ .github/workflows/build-apk.yml  # GitHub Actions 自动编译 APK
├─ .gitignore / .vercelignore
└─ 日程管家-发布包.zip          # 网页版发布包（不含 apk 大文件）
```

---

## 七、常见问题

- **后台/锁屏不提醒（华为/国产手机）**：按第二节的「华为重点」设置；务必允许自启动 + 后台活动 + 电池不受限 + 打开通知/横幅/声音。
- **点击铃铛测试**：应发一条「测试提醒」；如果没弹，检查是否已授权通知、系统通知开关是否打开。
- **想更响**：调大通知/铃声音量；渠道里可换更响的铃声。
- **APK 装不上/解析失败**：确认是 `app-debug.apk` 且来源可信；允许「未知来源」。
- **网页版国内打不开**：建议改用 APK（不依赖域名）。

---

*构建日期：2026-09-03 · 版本 v1.0*
